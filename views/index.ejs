<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/new">New Message</a>
    <% messages.forEach(message => { %>
    <p><%= message.text %></p>
    <p>
      <%= `- ${message.user} at ${message.createdAt.toLocaleString(undefined, {
      year: 'numeric', month: 'long', day: 'numeric', hour: 'numeric', minute:
      'numeric', hour12: false, timeZone:
      Intl.DateTimeFormat().resolvedOptions().timeZone, })} ` %>
    </p>
    <% }) %>
  </body>
</html>
